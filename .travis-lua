#!/bin/sh

git clone --quiet --depth 1 -b lua-5.1 https://github.com/LuaDist/lua.git lua.git
cd lua.git/
git checkout -qf $LUA_VERSION
cmake -DCMAKE_INSTALL_PREFIX=/usr .
make
sudo make install
cd ..

